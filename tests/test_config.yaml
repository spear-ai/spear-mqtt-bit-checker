sensors:
  ctd_temp:
    thresholds:
      min_temp_c: -10.0  #Different from default -6.0
      max_age_sec: 120.0  #Different from default 60.0
      #Leave other thresholds out - should use defaults

  bno_attitude:
    thresholds:
      max_age_sec: 100.0
      #Others should go to defaults

  acoustic_acsense_and_beamformer:
    thresholds: {}
    # Empty so this should use all the defaults